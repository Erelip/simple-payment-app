import React from 'react';
import { Route, Routes } from 'react-router-dom';
import { SignIn, SignUp} from '../';

const Auth: React.FC = () => {

  return (
    <div>
      <h1>Auth Page</h1>
    </div>
  );
};

export default Auth;